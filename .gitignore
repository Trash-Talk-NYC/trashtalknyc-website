# Local Netlify folder
.netlify

# Local env vars (never commit credentials)
.env
.env.local

# Dependencies
node_modules/

# Astro build output
dist/

# Astro type generation cache
.astro/

# Claude Code session memory (machine-local)
.claude/

# Agent skills and lock (tooling, not project files)
.agents/
skills-lock.json

# macOS metadata
.DS_Store
